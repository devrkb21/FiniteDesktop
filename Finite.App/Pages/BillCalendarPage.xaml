<Page x:Class="Finite.App.Pages.BillCalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:app="clr-namespace:Finite.App"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Loaded="OnLoaded">

    <Page.Resources>
        <app:BoolToTodayBrushConverter x:Key="TodayBrush" />
        <app:InMonthOpacityConverter x:Key="InMonthOpacity" />
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Bill Calendar" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Text="{Binding MonthTotalText}" Margin="20,0,0,0" VerticalAlignment="Bottom"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="◀" Command="{Binding PreviousMonthCommand}" Margin="0,0,6,0" />
                <ui:Button Content="Today" Command="{Binding GoTodayCommand}" Margin="0,0,6,0" />
                <ui:Button Content="▶" Command="{Binding NextMonthCommand}" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding MonthLabel}" FontSize="18" FontWeight="SemiBold" Margin="0,12,0,8" />

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Weekday header -->
            <UniformGrid Columns="7">
                <TextBlock Text="Mon" TextAlignment="Center" Opacity="0.6" FontSize="11" Margin="4" />
                <TextBlock Text="Tue" TextAlignment="Center" Opacity="0.6" FontSize="11" Margin="4" />
                <TextBlock Text="Wed" TextAlignment="Center" Opacity="0.6" FontSize="11" Margin="4" />
                <TextBlock Text="Thu" TextAlignment="Center" Opacity="0.6" FontSize="11" Margin="4" />
                <TextBlock Text="Fri" TextAlignment="Center" Opacity="0.6" FontSize="11" Margin="4" />
                <TextBlock Text="Sat" TextAlignment="Center" Opacity="0.6" FontSize="11" Margin="4" />
                <TextBlock Text="Sun" TextAlignment="Center" Opacity="0.6" FontSize="11" Margin="4" />
            </UniformGrid>

            <!-- Week rows: one full-width row per week; the 7-column split happens INSIDE -->
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Weeks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- NOTE: no UniformGrid wrapper here — it would squeeze the inner
                             7-column grid into 1/7 of the row width (the collapsed-calendar bug). -->
                        <ItemsControl ItemsSource="{Binding Days}" MinHeight="92">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="7" Rows="1" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="3" BorderBrush="#3394A3B8" BorderThickness="1" CornerRadius="6"
                                            Background="{Binding IsToday, Converter={StaticResource TodayBrush}}"
                                            Padding="6,4" ToolTip="{Binding DateFull}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding DayNumber}" FontSize="11" FontWeight="SemiBold"
                                                       Opacity="{Binding InMonth, Converter={StaticResource InMonthOpacity}}" />
                                            <ItemsControl ItemsSource="{Binding Items}" Margin="0,2,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="{Binding ChipBrush}" CornerRadius="3"
                                                                Padding="4,1" Margin="0,1">
                                                            <TextBlock Text="{Binding Text}" FontSize="9.5"
                                                                       Foreground="{Binding TextBrush}"
                                                                       TextTrimming="CharacterEllipsis" />
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</Page>
