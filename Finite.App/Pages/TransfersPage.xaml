<Page x:Class="Finite.App.Pages.TransfersPage"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         mc:Ignorable="d"
         Loaded="OnLoaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Transfers" FontSize="28" FontWeight="SemiBold" />

        <ui:Card Grid.Row="1" Margin="0,12,0,12" Padding="16">
            <StackPanel>
                <WrapPanel>
                    <ComboBox ItemsSource="{Binding AccountOptions}" SelectedItem="{Binding FromAccount}"
                              DisplayMemberPath="Name" Width="190" Margin="0,0,8,8" />
                    <ui:SymbolIcon Symbol="ArrowRight24" FontSize="20" Margin="0,0,8,8" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding AccountOptions}" SelectedItem="{Binding ToAccount}"
                              DisplayMemberPath="Name" Width="190" Margin="0,0,8,8" />
                    <ui:TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="Amount ৳" Width="130" Margin="0,0,8,8" />
                    <ui:TextBox Text="{Binding Fee, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="Fee ৳" Width="100" Margin="0,0,8,8" />
                    <ui:TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="Note (optional)" Width="180" Margin="0,0,8,8" />
                    <ui:Button Content="Transfer" Appearance="Primary" Command="{Binding AddCommand}" Margin="0,0,12,8" />
                    <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </WrapPanel>
            </StackPanel>
        </ui:Card>

        <ui:ListView Grid.Row="2" ItemsSource="{Binding Items}" SelectionMode="Single">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="30" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="90" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}" />
                        <TextBlock Grid.Column="1" Text="{Binding FromAccount.Name}" TextTrimming="CharacterEllipsis" />
                        <ui:SymbolIcon Grid.Column="2" Symbol="ArrowRight24" />
                        <TextBlock Grid.Column="3" Text="{Binding ToAccount.Name}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="4" Text="{Binding Description}" TextTrimming="CharacterEllipsis"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBlock Grid.Column="5" Text="{Binding Amount, StringFormat='৳ {0:N2}'}"
                                   HorizontalAlignment="Right" FontWeight="SemiBold" />
                        <ui:Button Grid.Column="6" Content="Delete" Appearance="Danger" Margin="8,0,0,0"
                                   Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ui:ListView}}"
                                   CommandParameter="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
    </Grid>
</Page>
