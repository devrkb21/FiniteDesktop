<Page x:Class="Finite.App.Pages.AnalyticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Finite.App.Controls"
      xmlns:app="clr-namespace:Finite.App"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Loaded="OnLoaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <Grid Margin="0,0,0,16">
                <TextBlock Text="Analytics &amp; Insights" FontSize="28" FontWeight="SemiBold" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <ui:Card Padding="14,8">
                        <StackPanel>
                            <TextBlock Text="FINANCIAL HEALTH" FontSize="10" Opacity="0.7" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding HealthScore}" FontSize="20" FontWeight="Bold" Foreground="{Binding HealthColor}" />
                                <TextBlock Text="{Binding HealthLabel}" FontSize="11" VerticalAlignment="Bottom" Margin="8,0,0,3" Opacity="0.8" />
                            </StackPanel>
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </Grid>

            <UniformGrid Columns="3" Margin="0,0,0,16">
                <ui:Card Margin="0,0,8,0" Padding="14">
                    <StackPanel>
                        <TextBlock Text="NET WORTH" FontSize="10" Opacity="0.7" />
                        <TextBlock Text="{Binding NetWorth}" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </ui:Card>
                <ui:Card Margin="8,0,8,0" Padding="14">
                    <StackPanel>
                        <TextBlock Text="DAILY AVG SPEND (THIS MONTH)" FontSize="10" Opacity="0.7" />
                        <TextBlock Text="{Binding DailyAverage}" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </ui:Card>
                <ui:Card Margin="8,0,0,0" Padding="14">
                    <StackPanel>
                        <TextBlock Text="SAVINGS RATE" FontSize="10" Opacity="0.7" />
                        <TextBlock Text="{Binding SavingsRate}" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </ui:Card>
            </UniformGrid>

            <ui:Card Padding="16" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Income vs Expense — Last 6 Months" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <controls:SimpleBarChart Height="220" Series="{Binding MonthlySeries}" Labels="{Binding MonthLabels}" />
                </StackPanel>
            </ui:Card>

            <ui:Card Padding="16" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Balance Trend — Last 30 Days" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <controls:SimpleLineChart Height="200" Values="{Binding TrendValues}" Labels="{Binding TrendLabels}" StrokeColor="#10B981" />
                </StackPanel>
            </ui:Card>

            <UniformGrid Columns="3">
                <ui:Card Margin="0,0,8,0" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Top Spending Categories (this month)" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="90" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontSize="12" TextTrimming="CharacterEllipsis" />
                                            <controls:ColoredProgressBar Height="4" Minimum="0"
                                                Maximum="{Binding DataContext.MaxCategoryTotal, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                Value="{Binding Total}" Foreground="{Binding Brush}" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding TotalText}" HorizontalAlignment="Right" FontSize="12" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ui:Card>

                <ui:Card Margin="8,0,8,0" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Top Merchants (this month)" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding Merchants}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="90" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" FontSize="12" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Grid.Column="1" Text="{Binding TotalText}" HorizontalAlignment="Right" FontSize="12" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ui:Card>

                <ui:Card Margin="8,0,0,0" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Budget Alerts" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <TextBlock Text="No budget alerts — all within limits." FontSize="12" Opacity="0.6"
                                   Visibility="{Binding HasNoAlerts, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <ItemsControl ItemsSource="{Binding BudgetAlerts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Text}" FontSize="12" Foreground="{Binding Color}" Margin="0,3" TextWrapping="Wrap" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ui:Card>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</Page>
